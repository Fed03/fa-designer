// eslint-disable-next-line no-unused-vars

class BaseEdge {
  /**
   * @param {string} pathDefinition
   */
  constructor(id, pathDefinition) {
    this.id = id;
    this.pathDefinition = pathDefinition;
  }
}

export { BaseEdge };
