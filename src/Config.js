const config = {
  nodeRadius: 50,
  edge: {
    strokeWidth: 5,
    creationId: "creation-edge"
  },
  markerSize: 4,
  dropShadowId: "drop-shadow-filter"
};

Object.freeze(config);

export default config;
