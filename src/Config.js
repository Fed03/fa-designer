const config = {
  nodeRadius: 50,
  edge: {
    stroke: "red",
    strokeWidth: 3,
    creationId: "creation-edge"
  },
  marker: {
    elementId: "arrowhead-marker",
    markerSize: 5
  }
};

Object.freeze(config);

export default config;
